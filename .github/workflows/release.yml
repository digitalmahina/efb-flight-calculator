name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build project
      run: |
        if [ -f "package.json" ] && grep -q '"build"' package.json; then
          npm run build
        fi
        
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          ## 🚀 EFB Calculator Release ${{ github.ref }}
          
          ### 📋 Изменения
          - Обновления и улучшения
          - Исправления ошибок
          - Новые функции
          
          ### 📦 Установка
          ```bash
          git clone https://github.com/digitalmahina/efb-flight-calculator.git
          cd efb-flight-calculator
          npm install
          ```
          
          ### 🔗 Ссылки
          - [Документация](https://github.com/digitalmahina/efb-flight-calculator#readme)
          - [Issues](https://github.com/digitalmahina/efb-flight-calculator/issues)
          
        draft: false
        prerelease: false
